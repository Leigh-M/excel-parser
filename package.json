{
  "name": "excel_parser",
  "version": "1.0.0",
  "description": "Excel parser reads, parses and writes excel .xslx files",
  "main": "excelParser.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "xlsx": "^0.16.6"
  }
}
